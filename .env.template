# ─── API endpoints ────
# Local development URLs 
# API_DIAGNO="http://localhost:8000"
# API_SOLUTIONS="http://localhost:9000"

# Replaced by deployed URLs in production
API_DIAGNO="https://user_name-spacename_diagno_api.hf.space"
API_SOLUTIONS="https://user_namet-space_name_treatment_api.hf.space"

# ─── Development flags ──────────────────────────────────────────────────────────
# MOCK=1 : runs the app without calling the APIs (useful for UI development)
# MOCK=0 : real API calls (requires API_DIAGNO and API_SOLUTIONS to be running)
MOCK=1

# DEBUG=1 : shows raw API payloads and LLM output in the UI
# DEBUG=0 : clean production UI
DEBUG=0
